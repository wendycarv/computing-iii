
#include <iostream>
#include <list>
#include <vector>

using namespace std;

int main(int argc, const char * argv[])
{
    vector<int> myList;
    
    myList.push_back(1);
    myList.push_back(2);
    myList.push_back(3);

    
    return 0;
}
